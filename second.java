import java.util.scanner;
Public class Swap
{
    Public Static Void Main(String args[])
    {
        int max;
        System.out.println("enter size of an array");
        n=sc.nextInt();
        int a[]=newInt();
        //redaing  elements from user
        System.out.println("enter array elements");
        for(int i=0;i<n;i++)
        {
            a[i]=sc.nextInt();
        }
        //sorting
        for(int i=0;in;i++)
        {
            if(a[i]>a[j])
            {
                int temp=a[i];
                a[i]=a[j];
                a[j]=temp;
            }
        }
        }
    }
}
